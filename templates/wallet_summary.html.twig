{#
/**
 * @file
 * Default theme implementation to display a (summary of a) wallet.
 * Variables:
 *   name //of the wallet
 *   url #to the wallet transaction listing
 *   currency (data object)
 *   balance (formatted)
 *   trades (number of)
 */
#}
<div class = "wallet-summary">
  <div class = "wallet-title">{{ name }}</div>
  <div class = "wallet-data">
  <a href="{{ url }}" title = "Show transactions" style = "font-size: 200%">{{ balance }}</a>
  <br />
  <a href="{{ url }}" title = "Show transactions">in {{ trades }} trades</a>
  </div>
</div><!--/wallet-summary-->