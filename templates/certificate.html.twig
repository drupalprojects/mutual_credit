{#
/**
 * @file
 * Default theme implementation to display a node.
 *
 * Available variables:
 * - node: Full mcapi_transaction entity including
  *   - created: unixtime of creation date. Preprocess functions can reformat it by
 *     calling format_date()
 * - payer: The formatted username of the payer
 * - payee: The formatted username of the payee
 * - worths: The formatted currency quantities
 * - state: The name of the transaction state
 * - type: The transaction type
 * - description: The one line description
 * - date: Formatted creation date
 * - attributes
 * -   attributes.class
 * - links: The transaction operations, formatted as a list
 * if the mcapi_limits module is enabled:
 * - pending
 * extra fieldAPI fields
 * - additional
 * child transactions, rendered as sentences
 * - children
#}



On {{ date }}, {{ payer }} paid {{ payee }} the sum of 
<br />{{ worths }} {{ links }}
<br />{{ additional }}

<br />
<br /><b>Testing variables:</b>
<br />pending: {{ pending }}
<br />children: {{ children }}
<br />