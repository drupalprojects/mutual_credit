{#
/**
 * @file
 * Default theme implementation to display a transaction certificate.
 *
 * Available variables:
 * - transaction: Full mcapi_transaction entity
 * - attributes
 * - created: unixtime of creation date. Preprocess functions can reformat it by
 *     calling format_date()
 *
 * - serial: The id of the transaction cluster
 * - payer: The payer wallet label
 * - payee: The payee wallet label
 * - creator: The formatted username of the user who entered the transaction
 * - worth: The formatted currency quantities
 * - state: The name of the transaction state
 * - type: The transaction type
 * - description: The one line description
 * - created: Formatted creation date
 * - changed: Formatted changed date
 * - attributes
 * -   attributes.class
 * - mcapi_signatures
 *     From the signatures module (if enabled)
 * - pending
 *     if the mcapi_limits module is enabled:
 * - field_example
 *     extra fieldAPI fields must be named specifically e.g
 * - children
 *     child transactions, rendered as sentences
#}

{% if mcapi_signatures %}{{ mcapi_signatures }}{% endif %}

<div class = "certificate">
  On {{ created }} <br />
  {{ payer }} paid {{ payee }} <br />
  the sum of <br />
  <font size=6>{{ worth }}</font><br />
  for: "<em>{{ description }}</em>"
</div>

<br />{% if children %}
<br /><b>Child transactions: </b>
<br />{{ children }}
<br />{% endif %}