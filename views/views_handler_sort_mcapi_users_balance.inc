<?php

class views_handler_sort_mcapi_users_balance extends views_handler_sort {
  function init() {
    //this is actually a tricky handler to write
    drupal_set_message("order by user balance hasn't been coded yet");
  }
}